<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorido.post.repository.PostLikeMapper">

    <insert id="save" parameterType="com.woorido.post.domain.PostLike">
        INSERT INTO woorido.post_likes (
            id, post_id, user_id, created_at
        ) VALUES (
            #{id}, #{postId}, #{userId}, #{createdAt}
        )
    </insert>

    <delete id="delete">
        DELETE FROM woorido.post_likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*)
        FROM woorido.post_likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </select>
    
    <select id="countByPostId" resultType="long">
        SELECT COUNT(*)
        FROM woorido.post_likes
        WHERE post_id = #{postId}
    </select>

</mapper>
