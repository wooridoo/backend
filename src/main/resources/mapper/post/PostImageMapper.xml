<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorido.post.repository.PostImageMapper">

    <insert id="save" parameterType="com.woorido.post.domain.PostImage">
        INSERT INTO woorido.post_images (
            id, post_id, image_url, display_order, created_at
        ) VALUES (
            #{id}, #{postId}, #{imageUrl}, #{displayOrder}, #{createdAt}
        )
    </insert>

    <select id="findAllByPostId" resultType="com.woorido.post.domain.PostImage">
        SELECT id, post_id, image_url, display_order, created_at
        FROM woorido.post_images
        WHERE post_id = #{postId}
        ORDER BY display_order ASC
    </select>

    <delete id="deleteAllByPostId">
        DELETE FROM woorido.post_images
        WHERE post_id = #{postId}
    </delete>

</mapper>
