<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorido.post.repository.CommentLikeMapper">

    <insert id="save" parameterType="com.woorido.post.domain.CommentLike">
        INSERT INTO woorido.comment_likes (
            id, comment_id, user_id, created_at
        ) VALUES (
            #{id}, #{commentId}, #{userId}, #{createdAt}
        )
    </insert>

    <delete id="delete">
        DELETE FROM woorido.comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>

    <delete id="deleteByCommentId">
        DELETE FROM woorido.comment_likes
        WHERE comment_id = #{commentId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*)
        FROM woorido.comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>

    <select id="countByCommentId" resultType="long">
        SELECT COUNT(*)
        FROM woorido.comment_likes
        WHERE comment_id = #{commentId}
    </select>

    <select id="findLikedCommentIds" resultType="string">
        SELECT comment_id
        FROM woorido.comment_likes
        WHERE user_id = #{userId}
        <if test="commentIds != null and commentIds.size() > 0">
            AND comment_id IN
            <foreach collection="commentIds" item="commentId" open="(" separator="," close=")">
                #{commentId}
            </foreach>
        </if>
    </select>

</mapper>
