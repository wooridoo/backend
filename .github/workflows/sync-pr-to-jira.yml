name: PR to Jira Transition
on:
  pull_request:
    types: [opened, reopened] # PR이 처음 생성되거나 다시 열렸을 때 실행

jobs:
  transition:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Jira
        uses: atlassian/gajira-login@v3
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}

      - name: Extract Jira Key from Branch
        id: parse
        run: |
          # 브랜치명에서 KAN-123 같은 키 추출
          BRANCH_NAME="${{ github.event.pull_request.head.ref }}"
          JIRA_KEY=$(echo "$BRANCH_NAME" | grep -oE '[A-Z]+-[0-9]+' | head -n 1)
          echo "jira_key=$JIRA_KEY" >> $GITHUB_OUTPUT

      - name: Transition Jira Issue
        if: steps.parse.outputs.jira_key != ''
        uses: atlassian/gajira-transition@v3
        with:
          issue: ${{ steps.parse.outputs.jira_key }}
          # 지라 워크플로우에 있는 실제 이름으로 설정해야 합니다. 
          # 아까 로그를 보니 '진행 중'은 확실히 있었습니다.
          transition: 👀 Review

      - name: Add PR Link Comment to Jira
        if: steps.parse.outputs.jira_key != ''
        uses: atlassian/gajira-comment@v3
        with:
          issue: ${{ steps.parse.outputs.jira_key }}
          comment: "🚀 PR이 생성되어 상태가 변경되었습니다. 바로가기: ${{ github.event.pull_request.html_url }}"
